<div class="field is-grouped is-grouped-right">
    <p class="control">
        <button type="submit" class="button is-{{ $primaryColor }}">
            {{ $primaryText }}
        </button>
    </p>
    <p class="control">
        <button type="reset" class="button is-{{ $secondaryColor }}">
            {{ $secondaryText }}
        </button>
    </p>
</div>
