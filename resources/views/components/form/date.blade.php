<div class="field">
    <label for="{{ $id }}" class="label">
        {{ $label }}
    </label>
    <div class="control">
        <input name="{{ $id }}" type="date" class="input" value="{{ $value }}">
    </div>
</div>
