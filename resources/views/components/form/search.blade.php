<div class="field has-addons">
    <div class="control">
        <input class="input" name="{{ $id }}" type="{{ $type }}" placeholder="{{ $placeholder }}" value="{{ $value }}">
    </div>
    <div class="control">
        <button type="submit" class="button is-primary">
            {{ __('form/shared.search') }}
        </button>
    </div>
</div>
